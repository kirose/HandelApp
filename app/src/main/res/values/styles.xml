<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Handel.EditText" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:backgroundTint">@color/colorAccent</item>
        <item name="android:textColorLink">@color/colorWhite</item>
        <item name="android:textColor">@color/colorWhite</item>
    </style>

    <style name="Handel.TextInputLayout" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColorHint">@color/colorAccent</item>
    </style>

    <style name="Handel.Button" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:background">@drawable/button</item>
        <item name="android:shadowColor">@color/colorPrimary</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="Handel.ProgressBar" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:progressDrawable">@drawable/progressbar</item>
    </style>

    <style name="Handel.ProgressBarDark" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:progressDrawable">@drawable/progressbar_dark</item>
    </style>
</resources>
